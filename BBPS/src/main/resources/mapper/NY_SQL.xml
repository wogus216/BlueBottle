<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="NY">
	<!-- 공지 카테고리 목록 불러오기 -->
	<select id="getCateList" resultType="hashmap">
	SELECT CATE_NO, CATE_NAME
	FROM NOTICE_CATE
	WHERE CATE_NO > 0 
	AND DEL_FLAG = 1		
	</select>
	
	<insert id="addCate" parameterType="hashmap">
	INSERT INTO NOTICE_CATE(CATE_NO, CATE_NAME, DEL_FLAG)
	VALUES(NOTICE_CATE_SEQ.NEXTVAL,#{inputTxt},1)
	</insert>
	
	<update id="updateCate" parameterType="hashmap">
	UPDATE NOTICE_CATE
	SET
	CATE_NAME = #{cateName}
	WHERE CATE_NO = #{cateNo}
	</update>
</mapper> 
